apiVersion: argoproj.io/v1alpha1
kind: Workflow
metadata:
  namespace: draftapp-dev
  generateName: hello-world-
spec:
  entrypoint: whalesay
  templates:
    - name: whalesay
      container:
        image: docker/whalesay
        command: [cowsay]
        args: ["hello world"]
